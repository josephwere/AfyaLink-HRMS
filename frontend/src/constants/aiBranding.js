// Default asset ships with the frontend build. Admins can override via System Settings.
// Keep the file name URL-safe to avoid routing/encoding issues in production.
export const DEFAULT_AI_ICON = "/neuroedge-logo.png";
