Backend tests:
- This folder contains placeholders for Jest/Mocha tests.
- To run tests: install jest and supertest, then implement tests.


To run tests locally: install dev dependencies in backend and run `npm test`. Jest + mongodb-memory-server are included for isolation.
